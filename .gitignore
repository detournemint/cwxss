__pycache__/
*.pyc

# ARRL code practice audio: 121 MB of somebody else's copyrighted recordings.
# fetch_arrl.py downloads it on demand, which is the right way round -- the
# script belongs to this project and the audio does not.
data/arrl/*.mp3
data/arrl/*.wav
data/arrl/*.txt

# Everything else is ours and is committed: the trained model the application
# needs to run, the checkpoints it came from, and the off-air recordings.
